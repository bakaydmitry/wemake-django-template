version: 2

python:
  version: 3.8
  install:
    - requirements: '{{cookiecutter.project_git_repo}}/docs/requirements.txt'

sphinx:
  configuration: '{{cookiecutter.project_git_repo}}/docs/conf.py'
  fail_on_warning: true
